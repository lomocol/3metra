<?php
/* Образец конфигурации PayAnyWay (MONETA.Assistant).
   Скопируйте в payanyway-config.php рядом с pay.php и впишите реальные
   значения. Сам payanyway-config.php в git не добавляется (см. .gitignore)
   и закрыт от чтения через веб (.htaccess) — код проверки целостности
   должен существовать только на сервере. */
return [
    /* Номер расширенного счёта (MNT_ID) из кабинета PayAnyWay */
    'mnt_id' => 'ВСТАВИТЬ_MNT_ID',

    /* «Код проверки целостности данных» из настроек счёта в кабинете */
    'integrity_code' => 'ВСТАВИТЬ_КОД_ПРОВЕРКИ_ЦЕЛОСТНОСТИ',

    /* 1 — тестовый режим (оплата не списывается), 0 — боевой.
       В боевом режиме уведомления с MNT_TEST_MODE=1 отклоняются */
    'test_mode' => 1,

    'currency' => 'RUB',

    /* Цены билетов — единственный источник правды для оплаты.
       Сумме из браузера сервер не доверяет */
    'prices' => [
        'm' => 2300, // мужской билет
        'f' => 2300, // женский билет
    ],

    /* Необязательно: перевод сделки в amoCRM после оплаты.
       Впишите ID статуса «Оплачено» (и ID воронки, если статус не в
       основной воронке). 0 — статус не менять, будет только примечание
       и бюджет сделки */
    'paid_status_id' => 0,
    'paid_pipeline_id' => 0,

    /* Необязательно: базовый URL сайта для Success/Fail URL.
       Пусто — определяется по домену запроса */
    'base_url' => '',

    /* Офлайн-цель Яндекс.Метрики после подтверждённой оплаты.
       OAuth-токену нужен доступ metrika:offline_data или metrika:write. */
    'metrika_counter_id' => 110737561,
    'metrika_payment_goal' => 'payment_success',
    'metrika_oauth_token' => 'ВСТАВИТЬ_OAUTH_ТОКЕН_МЕТРИКИ',
];
